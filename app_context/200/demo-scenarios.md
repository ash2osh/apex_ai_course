# App 200 Demo Scenarios

Use `MGR001` to claim and decide a direct-report manager task; confirm that another manager's request is unavailable. Use `HR001` to decide the second stage of a request longer than five working days. Use `ADMIN001` to demonstrate protected Users, Roles, and System Settings pages. Verify duplicate callbacks are rejected and balances change exactly once.
